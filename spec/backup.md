## Component: backup
```
$> ticat backup/to {tag-name}
$> ticat backup/mv {tag-name}
$> ticat backup/list|ls
{tag-name} {tag-name}
$> ticat backup/zip {tag-name}
$> ticat backup/restore {tag-name}
$> ticat backup/remove|rm {tag-name}
$> ticat backup/rename {old-tag-name} {new-tag-name}
```
